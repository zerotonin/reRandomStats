# stats.yaml 

# install: conda env create -f stats.yaml 
# update:  conda env update -f stats.yaml 
name: stats
dependencies:
  - python=3.7
  - pip
  - Shapely
  - ipython
  - pandas
  - numpy
  - scipy
  - tqdm
  - pip:
      - matplotlib
      - tables
      - statsmodels